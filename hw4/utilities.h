
//This will print out the individual bits of a 32-bit quantity (with a newline at the end)
void print_bits(unsigned int n);

//This will print out the individual bits of a 32-bit quantity (without a newline at the end)
void print_bits_no_newline(unsigned int n);

//This will print out the individual bits of a 64-bit quantity (with a newline at the end)
void print_bits64(unsigned long n);

